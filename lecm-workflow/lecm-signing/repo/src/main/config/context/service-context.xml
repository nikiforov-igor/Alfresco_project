<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:util="http://www.springframework.org/schema/util"
	   xmlns:p="http://www.springframework.org/schema/p"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						   http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

	<util:list id="signing.models">
		<value>alfresco/module/${artifactId}/models/lecm-signing-model.xml</value>
		<value>alfresco/module/${artifactId}/models/lecm-signing-result-model.xml</value>
	</util:list>
	<util:list id="signing.labels">
		<value>alfresco/module/${artifactId}/messages/lecm-signing</value>
		<value>alfresco/module/${artifactId}/messages/lecm-signing-result</value>
	</util:list>
	<bean id="signing.model.bootstrap" parent="dictionaryModelBootstrap"
		  depends-on="workflow.model.bootstrap"
		  p:models-ref="signing.models"
		  p:labels-ref="signing.labels"/>

	<util:constant id="ENGINE_ID" static-field="org.alfresco.repo.workflow.WorkflowDeployer.ENGINE_ID"/>
	<util:constant id="LOCATION" static-field="org.alfresco.repo.workflow.WorkflowDeployer.LOCATION"/>
	<util:constant id="MIMETYPE" static-field="org.alfresco.repo.workflow.WorkflowDeployer.MIMETYPE"/>
	<util:constant id="REDEPLOY" static-field="org.alfresco.repo.workflow.WorkflowDeployer.REDEPLOY"/>
	<util:list id="signing.workflows">
		<props>
			<prop key="#{ENGINE_ID}">activiti</prop>
			<prop key="#{LOCATION}">alfresco/module/${artifactId}/workflow/signing-workflow.bpmn20.xml</prop>
			<prop key="#{MIMETYPE}">text/xml</prop>
			<prop key="#{REDEPLOY}">${redeploy.process.definitions}</prop>
		</props>
	</util:list>
	<bean id="signing.workflow.bootstrap" parent="workflowDeployer"
		  depends-on="signing.model.bootstrap"
		  p:workflowDefinitions-ref="signing.workflows"/>

	<bean id="signingWorkflowService" parent="lecmWorkflowService"
		  depends-on="signing.workflow.bootstrap"
		  class="ru.it.lecm.workflow.signing.SigningWorkflowServiceImpl"
		  p:workCalendarService-ref="WorkCalendarService"
		  p:businessJournalService-ref="businessJournalService"/>
</beans>
