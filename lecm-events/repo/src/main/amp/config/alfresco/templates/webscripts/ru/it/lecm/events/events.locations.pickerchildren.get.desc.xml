<webscript>
  <shortname>Events Initiator PickerList</shortname>
  <description>Service to return metadata of child items in a parent space to display in the object finder.</description>
    <url>/lecm/events/locations/picker/{type}/{store_type}/{store_id}/{id}/children</url>
    <url>/lecm/events/locations/picker/{type}/children</url>
    <!-- The following 2 URLs are deprectated as of 4.0, the nodelocator service is now used by the picker -->
    <url>/lecm/events/locations/picker/{type}/{store_type}/{store_id}/{id}/siblings</url>
    <url>/lecm/events/locations/picker/{type}/{store_type}/{store_id}/{id}/doclib</url>
  <format default="json"/>
  <authentication>user</authentication>
  <transaction>none</transaction>
</webscript>
