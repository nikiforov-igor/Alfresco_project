<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:p="http://www.springframework.org/schema/p"
	   xmlns:context="http://www.springframework.org/schema/context"
	   xmlns:amq="http://activemq.apache.org/schema/core"
	   xmlns:jms="http://www.springframework.org/schema/jms"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
						http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
						http://activemq.apache.org/schema/core http://activemq.apache.org/schema/core/activemq-core.xsd
						http://www.springframework.org/schema/jms http://www.springframework.org/schema/jms/spring-jms.xsd">

	<context:component-scan base-package="ru.it.lecm.barcode.service"/>
	<context:property-placeholder ignore-unresolvable="true"
								  ignore-resource-not-found="true"
								  location="classpath:barcode-service.properties, classpath:alfresco-global.properties"/>

	<bean id="jmsFactory" class="org.apache.activemq.ActiveMQConnectionFactory" depends-on="broker" p:brokerURL="${barcode.brokerURL}"/>
	<amq:queue id="barcodeQueue" physicalName="barcodeQueue" />

	<bean id="cachedConnectionFactory"
		  class="org.springframework.jms.connection.CachingConnectionFactory"
		  p:targetConnectionFactory-ref="jmsFactory"
		  p:sessionCacheSize="10" />

	<jms:listener-container container-type="default"
							connection-factory="cachedConnectionFactory"
							acknowledge="client"
							concurrency="10-100"
							recovery-interval="5000">
		<jms:listener destination="barcodeQueue" ref="listener"/>
	</jms:listener-container>

	<bean id="listener" class="ru.it.lecm.barcode.service.service.BarcodeMessageListener"/>

	<amq:broker id="broker" useJmx="false" persistent="true" dataDirectory="${dir.root}/barcodeQueue">
		<amq:transportConnectors>
			<amq:transportConnector uri="${barcode.brokerURL}" />
		</amq:transportConnectors>
		<amq:persistenceAdapter>
			<amq:kahaDB directory="${dir.root}/barcodeQueue/Kaha"/>
		</amq:persistenceAdapter>
	</amq:broker>

	<bean id="producerTemplate"
		  class="org.springframework.jms.core.JmsTemplate"
		  p:connectionFactory-ref="cachedConnectionFactory"
		  p:defaultDestination-ref="barcodeQueue" />
</beans>
