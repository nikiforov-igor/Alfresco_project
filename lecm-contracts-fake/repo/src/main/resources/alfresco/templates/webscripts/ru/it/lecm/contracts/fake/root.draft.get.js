var node = contractsFake.getDraftRoot().getNodeRef().toString();
var draftPath = contractsFake.getDraftPath();
var documentPath = documentScript.getDocumentsPath();

model.nodeRef = node;
model.draftPath = draftPath;
model.documentPath = documentPath;