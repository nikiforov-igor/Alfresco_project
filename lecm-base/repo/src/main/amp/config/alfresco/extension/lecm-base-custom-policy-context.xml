<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE beans PUBLIC '-//SPRING//DTD BEAN//EN' 'http://www.springframework.org/dtd/spring-beans.dtd'>

<beans>
    <!-- for restore, needs to be before bootstrap-context -->
    <bean id="dictionaryModelType" class="org.alfresco.repo.dictionary.DictionaryModelType" init-method="init">
        <property name="dictionaryDAO" ref="dictionaryDAO"/>
        <property name="namespaceDAO" ref="namespaceDAO"/>
        <property name="nodeService" ref="nodeService"/>
        <property name="contentService" ref="contentService"/>
        <property name="policyComponent" ref="policyComponent"/>
        <!-- <property name="workflowService" ref="WorkflowService"/> -->
        <!-- <property name="searchService" ref="SearchService"/> -->
        <!-- <property name="namespaceService" ref="namespaceService"/> -->
        <property name="tenantService" ref="tenantService"/>
        <!-- <property name="tenantAdminService" ref="tenantAdminService"/> -->
        <property name="modelValidator" ref="modelValidator"/>
        <property name="transactionService" ref="TransactionService"/>
        <property name="jobLockService" ref="jobLockService"/>

        <!-- Diff. Инициализируем свойство для опционального отключения валидации инкрементальности обновления моделей-->
        <property name="doValidation" value="${dictionary.dictionaryModelType.doValidation}"/>

        <!-- <property name="storeUrls">
            <list>
                <value>${spaces.store}</value>
                <value>${spaces.archive.store}</value>
            </list>
        </property> -->
    </bean>

</beans>
