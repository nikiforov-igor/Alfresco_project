function main()
{
   var calendar = {
      id : "Calendar",
      name : "LogicECM.module.MiniCalendar",
      assignTo : "calendar",
      useOptions: false
   };
   model.widgets = [calendar];
}

main();
